import React from 'react';

import './NavBar.css';
import NavLinks from './NavLinks';

const NavBar = () => {
    return (
        <nav class='nav'>
            <NavLinks />
        </nav>
    );
};

export default NavBar;
